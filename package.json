{
  "name":"CHARLES",
  "version":"0.1.0",
  "dependencies": {
    "concurrently": "^3.5.1"
  },
  "scripts": {
    "start:server": "cd backend && yarn install && yarn start",
    "start:client": "cd client && yarn install yarn start",
    "start:dev": "yarn install && concurrently \"yarn start:server\" \"yarn start:client\""
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "author":"Travis DeMint"
}
